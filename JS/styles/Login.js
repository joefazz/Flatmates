import { Colors } from '../consts';

export const page = {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center'
}

export const pageFooter = {
    flex: 1,
    justifyContent: 'flex-start',
    alignItems: 'center',
}

export const mainContent = {
    flex: 2,
    alignItems: 'flex-start',
    justifyContent: 'center',
}

export const headingWrapper = {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
}

export const permissionsHeadingText = {
    fontWeight: '600',
    fontSize: 18,
    color: Colors.white,
    margin: 5,
}